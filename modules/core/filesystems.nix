{
  services = {
    # Automatic USB Mounting
    udisks2.enable = true;

    # File Manager Services
    gvfs.enable = true;
    tumbler.enable = true;

    # SSD Trim
    fstrim.enable = true;
  };
}
