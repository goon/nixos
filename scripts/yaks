#!/usr/bin/env bash
set -euo pipefail

cmd="${1:-}"; shift 2>/dev/null || true

notify() {
  if command -v notify-send >/dev/null; then
    notify-send "Yaks" "$1" \
      --app-name="Yaks" \
      --icon="system-software-update" \
      --urgency=low
  fi
}

cmd_reload() {
  if systemctl --user is-active --quiet yaks.service; then
    echo "Reloading Yaks QML engine..."
    if qs ipc call shell reload true; then
      notify "Shell reloaded successfully"
      echo "Done."
    else
      notify "IPC reload failed"
      echo "Error: IPC reload failed. Run 'systemctl --user restart yaks.service' to force a service restart."
      exit 1
    fi
  else
    echo "Yaks is not running. Starting it..."
    if systemctl --user start yaks.service; then
      notify "Shell started"
      echo "Done."
    else
      notify "Failed to start yaks"
      echo "Error: Failed to start yaks.service"
      exit 1
    fi
  fi
}

cmd_log() {
  stdbuf -oL journalctl --user -u yaks -f | awk '{
    gsub(/WARN|warn/, "\033[33m&\033[0m")
    gsub(/ERROR|error|FATAL|fatal/, "\033[31m&\033[0m")
    print
  }'
}

cmd_status() {
  systemctl --user status yaks -n 0 --no-pager
}

usage() {
  cat <<EOF
yaks — yaks management

Usage: yaks <command>

Commands:
  reload   Reload or start Quickshell (IPC reload if running, start if not)
  log      Tail the quickshell journal
  status   Show service status
EOF
}

case "$cmd" in
  reload)              cmd_reload ;;
  log)                 cmd_log ;;
  status)              cmd_status ;;
  -h|--help|"")        usage ;;
  *)                   echo "Unknown command: $cmd"; usage; exit 1 ;;
esac
