# ── OVERLAYS ──────────────────
# Self-authored nixpkgs overlays.
# Applied by lib/outputs.nix to all hosts.
# Add per-package files here, e.g.:
#   [ ./Hyprland.nix ]
# Each file should use the helpers from lib/overlays.nix so the override
# warns when it is no longer needed.
[ ]
